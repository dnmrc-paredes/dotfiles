return {
    {
        {'akinsho/toggleterm.nvim', version = "*", opts = {
            open_mapping = [[<c-\>]],
            size = 20,
            direction = 'float',
            persist_size = true,
            persist_mode = true,
        }}
    }
}
